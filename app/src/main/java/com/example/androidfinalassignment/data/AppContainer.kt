package com.example.androidfinalassignment.data

/**
 * A container for all the dependencies of the app.
 */
interface AppContainer {
    /*val mealRepository: MealsRepository*/
    val centralRepository: CentralRepository
}